# to start working we need to specify our name.

git config --global user.name "Qasem"
git config --global user.email "q.hashemi100@gmail.com"


# Git new Repository
git init
# set new default Repository as main => from now on every time i run the git init it will automaticly landing in main
git config --global init.defaultBranch main

# add to git Repository

git add <filename>  => add specific file
git add . => add all file in the directory
